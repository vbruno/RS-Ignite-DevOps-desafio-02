version: '3.8'

services:
  # mysql:
  #   image: mysql:8
  #   container_name: mysql
  #   volumes:
  #     - db:/var/lib/mysql
  #   ports:
  #     - 3306:3306
  #   environment:
  #     - MYSQL_ROOT_PASSWORD=root
  #     - MYSQL_DATABASE=rocketseat-db
  #     - MYSQL_USER=admin
  #     - MYSQL_PASSWORD=root
  #   networks:
  #     - primeira-network

  api-rocket:
    build:
      context: .
    container_name: rs-devops-desafio-02
    ports:
      - 3333:3333
    # depends_on:
    #   - mysql
    networks:
      - rs-devops-network

networks:
  rs-devops-network:
    driver: bridge

# volumes:
#   db:
